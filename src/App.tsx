import { Button } from "@/components/ui/button";

function App() {
  return (
    <main className="h-screen w-full bg-red-400 flex flex-row">
      selam naber
      <Button>selam</Button>
    </main>
  );
}

export default App;
